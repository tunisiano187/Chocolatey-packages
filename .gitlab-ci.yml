# Full project: https://gitlab.com/pages/plain-html
image: alpine:latest

pages:
  stage: deploy
  script:
  - mkdir public/script
  - cp *.user.js public/script/
  - sed -i 's/.version./2020.06.13.02/g' public/index.html
  artifacts:
    paths:
    - public
  only:
  - master
